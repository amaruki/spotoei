{
  "name": "spotoei",
  "version": "0.0.0",
  "private": true,
  "description": "SPOTOEI — keyboard-first, terminal-native Spotify client.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "start": "bun run --filter spotoei-tui start",
    "dev": "bun run --filter spotoei-tui dev",
    "build": "bun run --filter spotoei-protocol build && bun run --filter spotoei-tui build",
    "test": "bun test",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "typecheck": "bun run --filter spotoei-tui typecheck",
    "package": "bun run scripts/package.ts"
  },
  "devDependencies": {
    "@types/bun": "*",
    "@types/node": "^26.4.1",
    "oxfmt": "*",
    "oxlint": "*",
    "typescript": "*"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
